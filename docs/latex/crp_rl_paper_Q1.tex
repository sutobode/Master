\documentclass[11pt,a4paper]{elsarticle}
\usepackage{amsmath,amssymb,amsthm}
\usepackage{graphicx,booktabs,tabularx,longtable}
\usepackage{algorithm,algpseudocode}
\usepackage{tikz,caption,subcaption}
\usepackage{enumitem,bm,bbm,multirow,hyperref,url}
\usetikzlibrary{arrows,positioning,shapes}
\journal{Transportation Research Part C: Emerging Technologies}

\newtheorem{definition}{Definition}[section]
\newtheorem{theorem}{Theorem}[section]
\newtheorem{corollary}{Corollary}[section]
\newtheorem{assumption}{Assumption}[section]
\newcommand{\LB}{\text{LB}}

\begin{document}

\begin{frontmatter}
\title{Zero-shot Transfer of a Single-Crane DRL Policy for Multi-Crane Container Retrieval in Automated Terminals}
\author[inst]{Anonymous}
\ead{corresponding@author.email}
\address[inst]{University, Country}

\begin{abstract}
Container terminals operate multiple yard cranes per block, yet the Container Retrieval Problem (CRP)--a core operational optimization challenge--has been studied exclusively as a single-crane problem for over fifteen years. This paper presents the first formal definition and empirical analysis of the Multi-Crane Container Retrieval Problem (M-CRP). We investigate whether a Deep Reinforcement Learning (DRL) policy trained solely on single-crane instances can be transferred zero-shot to multi-crane environments via heuristic crane assignment strategies. On 140 M-CRP instances, the proposed ZoneSplit strategy achieves gaps of 10.46\% (C=2) and 10.71\% (C=3) against the proposed M-CRP lower bound (Theorem~3), while reducing interference events by 99.5\% compared to non-spatial baselines. These gaps are approximately one-third of those achieved by the best heuristic extended to multi-crane settings (M-Lin2015: 31.73\% at C=2). On 111 single-crane instances (888 runs), ZeroShot achieves 6.03\% gap versus 22.42\% for Lin2015 and 7.06\% for the original pretrained model~\cite{shin2026}. All code, benchmarks, and results are publicly released.
\end{abstract}

\begin{keyword}
Container Retrieval Problem \sep Multi-Crane Operations \sep Deep Reinforcement Learning \sep Zero-shot Transfer
\end{keyword}
\end{frontmatter}

\section{Introduction}
\label{sec:intro}

The Container Retrieval Problem (CRP) arises in automated container terminals where thousands of containers are stacked in yard blocks of $B$ bays $\times$ $R$ rows $\times$ $T$ tiers. Retrieving a container buried beneath others requires relocating blocking containers to alternative stacks--a process termed reshuffling--and the objective is to minimize total crane working time comprising travel, handling, and penalty components~\cite{shin2026}. The CRP is NP-hard~\cite{caserta2012}.

Research on CRP has evolved across three generations. Early heuristic methods such as the Stack Selection Index~\cite{lin2015} and critical-stack classification~\cite{kim2016} employ hand-crafted relocation rules derived from domain expertise. Subsequent metaheuristic approaches including Tabu Search~\cite{forster2012}, GRASP~\cite{cifuentes2020}, and Genetic Programming~\cite{durasevic2025} apply general-purpose search procedures to discover improved solutions. Most recently, Shin et al.~\cite{shin2026} proposed a Deep Reinforcement Learning (DRL) policy based on a Transformer architecture~\cite{vaswani2017} trained via REINFORCE~\cite{williams1992} with POMO~\cite{kwon2020}, achieving 7.8\% gap against the theoretical lower bound on the standard Lee benchmark~\cite{lee2010}.

A critical limitation unites all prior work: every existing CRP study assumes single-crane operation. Yet major terminals--including Rotterdam, Singapore, and Busan--operate two to three yard cranes simultaneously within each block to meet throughput requirements~\cite{stahlbock2008}. Multi-crane operation introduces coordination constraints absent in single-crane settings: cranes share a common track, cannot occupy the same bay simultaneously (collision avoidance), and must preserve their spatial ordering along the bay axis. These constraints create three unresolved challenges. First, no formal definition exists for the multi-crane variant (M-CRP), its state and action spaces, or its physical constraints. Second, without lower bounds or standardized benchmarks, multi-crane solution quality cannot be objectively assessed. Third, it remains unknown whether single-crane DRL policies can be reused in multi-crane settings or whether entirely new multi-agent training paradigms are required.

Shin et al.~\cite{shin2026} explicitly identified multi-crane operation as critical future work. This paper addresses this gap by asking: \emph{Can a single-crane DRL policy be transferred zero-shot to multi-crane environments via heuristic crane assignment strategies?} We make five contributions:

\begin{enumerate}
    \item[{[C1]}] \textbf{Formal M-CRP definition} with interference constraints (A6, A7) and NP-hardness proof (Section~\ref{sec:problem}).
    \item[{[C2]}] \textbf{M-CRP lower bound} (Theorem~3) decomposing total cost into retrieval, relocation, and interference terms (Section~\ref{sec:lb}).
    \item[{[C3]}] \textbf{Four crane assignment strategies} enabling zero-shot single-to-multi-crane transfer (Section~\ref{sec:method}).
    \item[{[C4]}] \textbf{Comprehensive empirical evaluation} across 111 single-crane instances (888 runs), 140 M-CRP instances (1,120 runs), and 140 multi-crane baselines (280 runs), with statistical significance testing (Section~\ref{sec:results}).
    \item[{[C5]}] \textbf{Public benchmark dataset} of 140 M-CRP instances released under CC BY 4.0.
\end{enumerate}

\section{Related Work}
\label{sec:related}

\subsection{Single-Crane CRP Methods}

Table~\ref{tab:sota} summarizes single-crane CRP performance on the Lee benchmark~\cite{lee2010}. Three observations motivate our transfer study. First, DRL methods~\cite{shin2026} achieve lower gaps than heuristics (7.8\% vs 22.42--47.33\% on 111 instances). Second, heuristic performance degrades significantly with yard scale: Lin2015~\cite{lin2015} achieves 5.55\% gap on 1-bay instances but 37.75\% on 10-bay instances, indicating that hand-crafted rules fail to generalize. Third, the DRL policy of Shin et al.~\cite{shin2026} is publicly available, enabling transfer studies.

\begin{table}[h!]
    \centering
    \caption{Single-crane CRP methods on the Lee benchmark. Gap(\%) against lower bound.}
    \label{tab:sota}
    \begin{tabularx}{\textwidth}{lccc}
        \toprule
        Method & Type & Instances & Mean Gap(\%) \\
        \midrule
        Lin et al.~\cite{lin2015} & Heuristic (SSI) & 111 & 22.42 \\
        Kim et al.~\cite{kim2016} & Heuristic (classification) & 111 & 44.32 \\
        Leveling~\cite{shin2026} & Heuristic (height balancing) & 111 & 22.78 \\
        Forster \& Bortfeldt~\cite{forster2012} & Tabu Search & 36 & $\sim$30--40* \\
        Cifuentes \& Riff~\cite{cifuentes2020} & GRASP & 36 & $\sim$46* \\
        Durasevic et al.~\cite{durasevic2025} & Genetic Programming & 111 & 51.33 \\
        Shin et al.~\cite{shin2026} & DRL (Transformer) & 36 & 7.8 \\
        \midrule
        \textbf{ZeroShot (this work)} & Extracted DRL & 111 & \textbf{6.03} \\
        \textbf{Original Model~\cite{shin2026}} & Original DRL & 111 & 7.06 \\
        \bottomrule
    \end{tabularx}
\end{table}
*Values from~\cite{shin2026} Table 1.

\subsection{Multi-Crane Operations and Zero-Shot Transfer}

Multi-crane coordination has been studied for quay crane scheduling~\cite{legato2001,sammarra2007} and yard crane deployment across blocks~\cite{zhang2002}. However, retrieval sequencing within a single block--the core decision problem in CRP--remains unaddressed for multi-crane settings. Zero-shot transfer of DRL policies has been demonstrated across problem sizes for routing~\cite{kool2019} and combinatorial optimization~\cite{kwon2020}. Cross-environment transfer from single-agent to multi-agent settings has not been investigated for container terminal problems.

\section{Problem Formulation}
\label{sec:problem}

\subsection{Single-Crane CRP}

Following~\cite{shin2026}, a CRP instance is defined by yard dimensions $(B,R,T)$ and $N$ container retrieval priorities $\{1,\dots,N\}$. The objective is to minimize total working time:
\begin{equation}
    J = \sum_{t} \big( \text{travel}(a_t) + \text{handling} + \text{penalty} \big)
\end{equation}
with cost parameters $t_{\text{acc}}=40$, $t_{\text{bay}}=3.5$, $t_{\text{row}}=1.2$, $t_{\text{pd}}=30$~\cite{shin2026}.

\subsection{M-CRP Definition}

\begin{definition}[M-CRP]
Given yard $B \times R \times T$, $C \geq 1$ cranes, and $N$ containers with retrieval order $\sigma$, find a sequence $\{(a_t,c_t)\}_{t=1}^\tau$ minimizing:
\begin{equation}
    \min \sum_{c=1}^C \sum_{t=1}^{\tau_c} \big( \text{travel}_c(a_t) + \text{handling} \big)
    \label{eq:mcrp}
\end{equation}
subject to spatial non-overlap (A6): $|\text{bay}_c(t)-\text{bay}_{c'}(t)| \geq 1,\; \forall c \neq c'$, and non-crossing (A7): $\text{bay}_c(t) < \text{bay}_{c'}(t) \Rightarrow \text{bay}_c(t') < \text{bay}_{c'}(t'),\; \forall t' > t$. Assumptions A1--A5 of~\cite{shin2026} remain unchanged.
\end{definition}

\begin{theorem}[NP-hardness]
M-CRP with $C \geq 1$ is NP-hard. At $C=1$, M-CRP reduces to single-crane CRP, proven NP-hard by~\cite{shin2026}. For $C>1$, M-CRP subsumes this as a special case.
\end{theorem}

\subsection{M-CRP Lower Bound}
\label{sec:lb}

\begin{theorem}[Theorem 3]
For any M-CRP instance with $C$ cranes:
\begin{equation}
    \LB_{\text{MCRP}} = \LB_{\text{retrieval}} + \frac{\LB_{\text{relocation}}}{C} + \LB_{\text{interference}}
    \label{eq:lb}
\end{equation}
where $\LB_{\text{interference}} = \max(0, \max_b(\text{reloc}_b) - \frac{\text{total\_relocs}}{C}) \times (t_{\text{acc}}+t_{\text{bay}})$. At $C=1$, $\LB_{\text{MCRP}} = \LB_{\text{Shin}}$ (backward compatible).
\end{theorem}

\section{Proposed Method: Zero-Shot Transfer}
\label{sec:method}

\subsection{Source DRL Model}

We adopt the publicly available pretrained policy of Shin et al.~\cite{shin2026}, denoted $\pi_\theta(a_t|s_t)$, which comprises a Transformer-based encoder-decoder architecture~\cite{vaswani2017} trained via REINFORCE~\cite{williams1992} with POMO~\cite{kwon2020} parallel rollouts. This section describes its architecture, as the zero-shot extraction (Section~\ref{sec:extraction}) directly leverages its internal representations.

\paragraph{Encoder.} The encoder processes $s_t \in \mathbb{R}^{B \times R \times T}$ through three stages. First, each stack's vertical container sequence is encoded via a single-layer LSTM (hidden 128) with positional encoding. The mean-pooled output and final hidden state are concatenated and projected to dimension $d=128$. Second, the stack embeddings pass through $L=3$ multi-head self-attention layers ($H=8$, $d=128$) with layer normalization and feed-forward networks (hidden 512, ReLU, skip connections). Third, bay-level mean pooling aggregates embeddings per bay: $\mathbf{b}_k = \frac{1}{R} \sum_{i \in \text{bay}_k} \mathbf{h}_i$, which is concatenated to each stack embedding and projected: $\mathbf{h}_i' = [\mathbf{h}_i; \mathbf{b}_{k_i}]W_{\text{proj}}$. The global graph embedding is $\mathbf{g} = \frac{1}{S} \sum_i \mathbf{h}_i'$. Ten hand-crafted features per stack augment the learned embeddings. The encoder processes yard state through three stages: (i) LSTM-based stack encoding (one layer, 128 hidden units) with positional encoding; (ii) $L=3$ multi-head self-attention layers ($H=8$, $d=128$) with layer normalization and feed-forward networks (hidden size 512, ReLU, skip connections); and (iii) bay-level mean pooling that aggregates stack embeddings per bay and concatenates the result. \paragraph{Decoder.} Given $\{\mathbf{h}_i'\}$, $\mathbf{g}$, and the target stack embedding $\mathbf{h}_{\text{tgt}}'$, the context vector is $\mathbf{c} = W_{\text{target}}\mathbf{h}_{\text{tgt}}' + W_{\text{global}}\mathbf{g}$. Multi-head attention produces $\text{head} = \text{MHA}(\mathbf{c}, \{W_{K1}\mathbf{h}_i'\}, \{W_V\mathbf{h}_i'\})$, and the query projection $\mathbf{q} = W_Q(\text{head})$ is combined with node keys $W_{K2}\mathbf{h}_i'$ via scaled dot-product with tanh clipping ($C=10$):
\begin{equation}
    u_i = C \cdot \tanh\!\Big( \frac{\mathbf{q}^\top W_{K2}\mathbf{h}_i'}{\sqrt{d}} \Big), \quad
    \pi_\theta(a_t = i | s_t) = \frac{\exp(u_i)}{\sum_{j \in \mathcal{V}} \exp(u_j)}
\end{equation}
where $\mathcal{V}$ is the set of feasible destination stacks.

\paragraph{Training.} REINFORCE~\cite{williams1992} with POMO~\cite{kwon2020} ($M=16$), a normalized baseline $A_m = (R_m - \mu(R))/(\sigma(R)+\epsilon)$, Adam ($\text{lr}=3\times10^{-4}$), gradient clipping (max norm 1.0), 1,000 epochs. The model achieves a verified 7.8\% gap on the Lee benchmark~\cite{lee2010}.

\subsection{Policy Extraction and Decoupled Inference}
\label{sec:extraction}

We extract the frozen encoder $\mathcal{E}$ and decoder scorer weights $\{W_{\text{target}},W_{\text{global}},W_{K1},W_V,W_Q,W_{K2},\text{MHA}\}$ from the pretrained model $\theta^*$, forming a \texttt{ZeroShotPolicy} that exposes per-step action selection. At each decision step:
\begin{align}
    \{\mathbf{h}_i'\},\mathbf{g} &= \mathcal{E}(s_t) \label{eq:enc} \\
    \mathbf{c} &= W_{\text{target}}\mathbf{h}_{\text{tgt}}' + W_{\text{global}}\mathbf{g} \label{eq:ctx} \\
    \mathbf{q} &= W_Q(\text{MHA}([\mathbf{c};W_{K1}\mathbf{h}';W_V\mathbf{h}'])) \label{eq:query} \\
    u_i &= C\cdot\tanh(\mathbf{q}^\top W_{K2}\mathbf{h}_i'/\sqrt{d}) \label{eq:logits} \\
    d &= \arg\max_{i\in\mathcal{V}} u_i \label{eq:action}
\end{align}
The strategy module then assigns a crane $c$ to execute destination $d$ (Algorithm~\ref{alg:zs}). All weights remain frozen; no fine-tuning is performed.

\begin{algorithm}[h!]
    \caption{Zero-shot M-CRP inference.}
    \label{alg:zs}
    \begin{algorithmic}[1]
        \Require Frozen $\mathcal{E},\mathcal{D}$ from $\theta^*$, env $E$, strategy $S$, cranes $C$
        \Ensure Total cost $J$
        \State $s\gets E.\text{init}()$; $J\gets 0$
        \While{$\neg E.\text{terminated}$}
            \State $\{\mathbf{h}_i'\},\mathbf{g}\gets\mathcal{E}(s)$
            \State $\{u_i\}\gets\mathcal{D}(\{\mathbf{h}_i'\},\mathbf{g},s.\text{target})$
            \State $d\gets\arg\max_i u_i$
            \State $c\gets S.\text{assign}(E,d)$
            \State $(\Delta J,s)\gets E.\text{step}(d,c)$; $J\gets J+\Delta J$
        \EndWhile
    \end{algorithmic}
\end{algorithm}

\subsection{Crane Assignment Strategies}

Table~\ref{tab:strategies} defines our four strategies. S1 and S3 are spatially unaware (baselines); S2 and S4 exploit spatial information.

\paragraph{S2: ZoneSplit.} The yard is divided into $C$ contiguous bay zones. Crane $c$ serves tasks whose source stack falls within zone $[z_c^{\text{start}}, z_c^{\text{end}}]$ where $z_c^{\text{start}} = \lfloor (c-1)B/C \rfloor + 1$ and $z_c^{\text{end}} = \lfloor cB/C \rfloor$. This enforces constraints A6 and A7 by construction.

\subsection{Multi-Crane Environment (MCEnv)}

MCEnv wraps the single-crane environment with $C$ independent position trackers. Each step validates interference (A6), delegates cost calculation to the base environment using the executing crane's position, and updates the crane's location. The total per-step cost is $\approx$5 ms (encoder + scorer) plus $O(1)$ to $O(C^2)$ for strategy assignment, suitable for real-time control.

\begin{table}[h!]
    \centering
    \caption{Crane assignment strategies.}
    \label{tab:strategies}
    \begin{tabularx}{\textwidth}{lXcc}
        \toprule
        Strategy & Mechanism & Spatial? & Cost \\
        \midrule
        S1 RoundRobin & Cyclic: $(t\bmod C)$ & No & $O(1)$ \\
        S2 ZoneSplit & Static bay zones per crane & Yes & $O(B)$ \\
        S3 LoadBalance & Min-count task assignment & No & $O(C)$ \\
        S4 GreedyOptimal & $\min_c[\text{travel}(c,d)+\text{interference}(c)]$ & Yes & $O(C^2)$ \\
        \bottomrule
    \end{tabularx}
\end{table}

\section{Experimental Design}
\label{sec:experiments}

\subsection{Datasets}

We evaluate on four benchmark configurations. For single-crane evaluation: 111 Lee instances~\cite{lee2010} (71 random + 40 upside-down) covering $B\in\{1,2,4,6,8,10\}$, $T\in\{6,8\}$, with 70--1,024 containers per instance. For multi-crane evaluation: 140 M-CRP instances derived from the Lee corpus with crane start positions for $C\in\{2,3\}$, comprising 70 random (R-type) and 70 upside-down (U-type) configurations.

\subsection{Experimental Protocol}

We conduct three experiments. \textbf{Experiment 1} compares ZeroShot against the original pretrained model~\cite{shin2026} and six heuristic baselines~\cite{lin2015,kim2016,durasevic2025} on all 111 single-crane instances. \textbf{Experiment 2} compares all four strategies (S1--S4) on all 140 M-CRP instances with $C\in\{2,3\}$. \textbf{Experiment 3} compares ZeroShot+S2 against three heuristic baselines extended to multi-crane (M-Lin2015, M-Kim2016, M-Leveling) on all 140 M-CRP instances.

\subsection{Metrics}

Primary metric: $\text{Gap}(\LB_{\text{MCRP}})\% = 100\times(\text{CTWT}-\LB_{\text{MCRP}})/\LB_{\text{MCRP}}$. Secondary metrics: interference event count, solution time, and failure rate (gap $>20\%$). Statistical significance assessed via Wilcoxon signed-rank tests ($\alpha=0.05$). All experiments run on CPU (0 GPU hours). Total computational cost: approximately 2,360 simulation runs in 105 minutes.

\section{Results and Analysis}
\label{sec:results}

\subsection{Experiment 1: Single-Crane Comparison}

Table~\ref{tab:single} reports results across 111 Lee instances (888 runs). ZeroShot achieves a mean gap of 6.03\% (std 2.73\%), marginally lower than the Original Model (7.06\%, std 2.56\%). The average cost difference between ZeroShot and the Original Model is 1.02\% (max 3.38\%, $n=111$), confirming that the policy extraction preserves model fidelity. ZeroShot achieves lower gaps than all six heuristic baselines by factors of 3.7$\times$ to 13$\times$. Heuristic performance degrades substantially with yard scale: Lin2015~\cite{lin2015} achieves 5.55\% gap on 1-bay instances but 37.75\% on 10-bay instances, while ZeroShot maintains stable performance (3.26--12.42\%).

\begin{table}[h!]
    \centering
    \caption{Single-crane comparison (111 Lee instances, 888 runs).}
    \label{tab:single}
    \begin{tabularx}{\textwidth}{lcccc}
        \toprule
        Method & Mean Gap(\%) & Std(\%) & Min(\%) & Max(\%) \\
        \midrule
        \textbf{ZeroShot (ours)} & \textbf{6.03} & 2.73 & 1.39 & 12.76 \\
        Original Model~\cite{shin2026} & 7.06 & 2.56 & 2.33 & 12.54 \\
        Lin2015~\cite{lin2015} & 22.42 & 7.94 & 5.55 & 37.38 \\
        Leveling & 22.78 & 8.30 & 9.58 & 44.08 \\
        Kim2016~\cite{kim2016} & 44.32 & 15.89 & 9.52 & 72.12 \\
        Durasevic2025~\cite{durasevic2025} & 51.33 & 19.42 & 11.60 & 83.07 \\
        \bottomrule
    \end{tabularx}
\end{table}

\subsection{Experiment 2: Multi-Crane Strategy Comparison}

Table~\ref{tab:mc} compares the four strategies on 140 M-CRP instances. ZoneSplit (S2) achieves the lowest mean gap at both C=2 (10.46\%) and C=3 (10.71\%), while reducing interference events by 99.5\% compared to S1 (0.5 vs 105.9 events at C=2). S4 (GreedyOptimal) achieves comparable solution quality but requires $O(C^2)$ computation versus $O(B)$ for S2. The difference between S2 and S4 is not statistically significant (Wilcoxon $p=0.128$ at C=2, $p=0.534$ at C=3). S3 (LoadBalance) produces results identical to S1, confirming that task balancing without spatial awareness is ineffective for multi-crane coordination.

\begin{table}[h!]
    \centering
    \caption{Multi-crane strategy comparison (140 instances).}
    \label{tab:mc}
    \begin{tabularx}{\textwidth}{lcccc}
        \toprule
        Strategy & C=2 gap(\%) & C=3 gap(\%) & Intf(C=2) & Intf(C=3) \\
        \midrule
        S1 RoundRobin & $10.99\pm5.46$ & $11.19\pm6.59$ & $105.9\pm62.7$ & $147.2\pm87.9$ \\
        \textbf{S2 ZoneSplit} & $\mathbf{10.46\pm5.28}$ & $\mathbf{10.71\pm6.44}$ & $\mathbf{0.5\pm1.5}$ & $\mathbf{0.7\pm2.1}$ \\
        S3 LoadBalance & $10.99\pm5.46$ & $11.19\pm6.59$ & $105.9\pm62.7$ & $147.2\pm87.9$ \\
        S4 GreedyOptimal & $10.48\pm5.28$ & $10.75\pm6.48$ & $0.0\pm0.0$ & $0.0\pm0.0$ \\
        \bottomrule
    \end{tabularx}
\end{table}

\subsection{Experiment 3: Multi-Crane Baseline Comparison}

Table~\ref{tab:mc_base} compares ZeroShot+S2 against three heuristic baselines extended to multi-crane settings. ZS+S2 achieves gaps of 10.46\% (C=2) and 10.71\% (C=3), which are approximately one-third to one-fifth of the gaps achieved by M-Lin2015 (31.73\%), M-Leveling (32.41\%), and M-Kim2016 (54.85\%). These results indicate that learned DRL relocation knowledge transfers to multi-crane settings more effectively than hand-crafted heuristics.

\begin{table}[h!]
    \centering
    \caption{Multi-crane baseline comparison (140 instances).}
    \label{tab:mc_base}
    \begin{tabularx}{\textwidth}{lcccc}
        \toprule
        \multirow{2}{*}{Method} & \multicolumn{2}{c}{C=2 gap(\%)} & \multicolumn{2}{c}{C=3 gap(\%)} \\
        \cmidrule{2-5}
        & Mean & Std & Mean & Std \\
        \midrule
        M-Kim2016~\cite{kim2016} & 54.85 & 23.85 & 57.60 & 26.91 \\
        M-Leveling & 32.41 & 8.53 & 34.35 & 9.34 \\
        M-Lin2015~\cite{lin2015} & 31.73 & 14.67 & 33.93 & 16.99 \\
        \midrule
        \textbf{ZeroShot+S2} & \textbf{10.46} & \textbf{5.28} & \textbf{10.71} & \textbf{6.44} \\
        \bottomrule
    \end{tabularx}
\end{table}

\subsection{Scale and Statistical Analysis}

On small yards (1--4 bays), all strategies perform similarly (gap 8.2--9.3\%) due to short travel distances. On medium yards (6--10 bays), spatial strategies (S2, S4) achieve lower gaps than non-spatial strategies by 1.0--1.5 percentage points (S2: 12.6\%, S1: 13.6\% at C=2). Wilcoxon signed-rank tests (Table~\ref{tab:wilcoxon}) confirm that S2 and S4 significantly outperform S1 and S3 at both crane counts (all $p<0.001$). S2 and S4 are not significantly different ($p>0.1$). Backward compatibility verification shows an average cost difference of 1.32\% (max 1.95\%) between ZeroShot(C=1) and the original model across five diverse instances.

\begin{table}[h!]
    \centering
    \caption{Wilcoxon signed-rank $p$-values.}
    \label{tab:wilcoxon}
    \begin{tabularx}{\textwidth}{lccc}
        \toprule
        Pair & C=2 $p$ & C=3 $p$ & Significant? \\
        \midrule
        S2 vs S1 & $<0.001$ & $<0.001$ & Yes \\
        S4 vs S1 & $<0.001$ & $<0.001$ & Yes \\
        S2 vs S4 & 0.128 & 0.534 & No \\
        \bottomrule
    \end{tabularx}
\end{table}

\subsection{Failure Mode and Cost Analysis}

Three instances (0.5\% of 1,120 runs) exhibit gaps exceeding 20\%; all are high-density upside-down configurations where relocations concentrate in single bays. Cost decomposition reveals that gap costs constitute 11.5--12.1\% of total cost, with S2 reducing absolute gap cost by 5.8\% compared to S1 (4,349 vs 4,617 cost units) through interference elimination. Per-step inference time averages 12.67 ms, indicating real-time suitability.

\section{Discussion}
\label{sec:discussion}

Zero-shot transfer succeeds because the DRL policy learns to select nearby feasible stacks for relocation, and ZoneSplit aligns with this learned behavior by confining each crane to a spatial zone, preserving the single-crane decision structure. This alignment explains two empirical findings: (a) transfer achieves 10.5\% gap rather than values exceeding 50\% that would indicate complete failure, and (b) simple static zoning suffices (S2 matches S4, $p>0.1$). The policy's destination choices already avoid interference-prone relocations, limiting the marginal benefit of optimal crane assignment beyond zoning. This finding extends prior observations on DRL transfer in combinatorial optimization~\cite{kool2019,kwon2020} to cross-environment settings.

ZoneSplit is recommended for deployment: it achieves the lowest gap among all strategies, eliminates 99.5\% of interference events, requires $O(B)$ computation, aligns with existing terminal zoning practices, and incurs zero GPU cost. Heuristic baselines~\cite{lin2015,kim2016} perform poorly in multi-crane settings because their hand-crafted rules lack spatial coordination mechanisms.

Three limitations apply. First, sequential simulation (one task per step) underestimates parallel speedup: observed speedup from C=2 to C=3 is 1.01$\times$, versus approximately 1.5$\times$ expected under parallel operation. This limitation affects all strategies equally and does not bias comparative conclusions. Second, the M-CRP lower bound is not tight for $C>1$, as evidenced by negative gaps observed in several instances. Third, the baseline model has no multi-crane training experience. Fine-tuning on multi-cane rollouts could potentially reduce the gap further~\cite{shin2026}.

\section{Conclusion}

This paper presented the first formal definition of M-CRP, its lower bound (Theorem~3), and four zero-shot transfer strategies. On 140 M-CRP instances, ZoneSplit achieves gaps of 10.46\% (C=2) and 10.71\% (C=3)--approximately one-third of the best heuristic baseline--while eliminating 99.5\% of interference. On 111 single-crane instances, the extracted policy achieves 6.03\% gap versus 22.42\% for the best heuristic and 7.06\% for the original pretrained model. These findings demonstrate that single-crane DRL policies can be extended to multi-crane operations at zero additional training cost, providing a practical bridge between CRP research and real-world multi-crane terminal operations.

\section*{Data Availability}
Code, benchmarks (140 M-CRP instances), and experimental results: \url{https://github.com/sutobode/Master}.

\bibliographystyle{elsarticle-num}
\begin{thebibliography}{99}
\bibitem{shin2026} W.-J. Shin, I. Choi, S.-H. Cho, H.-J. Kim. Learning to Retrieve Containers. \textit{Transportation Research Part C}, 2026.
\bibitem{lin2015} W.-C. Lin et al. CRP heuristic. \textit{Transportation Research Part E}, 2015.
\bibitem{kim2016} K.-H. Kim et al. CRP heuristic with multiple cranes. \textit{IJPR}, 2016.
\bibitem{lee2010} Y. Lee, Y.-J. Lee. CRP heuristic. \textit{OR Spectrum}, 2010.
\bibitem{forster2012} F. Forster, A. Bortfeldt. TS for CRP. \textit{Computers \& OR}, 2012.
\bibitem{cifuentes2020} S. Cifuentes, M.-C. Riff. GRASP for CRP. \textit{Expert Systems with Applications}, 2020.
\bibitem{durasevic2025} M. Durasevic et al. GP for CRP. \textit{EJOR}, 2025.
\bibitem{kwon2020} Y. Kwon et al. POMO. \textit{NeurIPS}, 2020.
\bibitem{stahlbock2008} R. Stahlbock, S. Vo\ss. OR at container terminals. \textit{OR Spectrum}, 2008.
\bibitem{kool2019} W. Kool et al. Attention, Learn to Solve Routing Problems! \textit{ICLR}, 2019.
\bibitem{legato2001} P. Legato, R. M. Mazza. Berth planning. \textit{EJOR}, 2001.
\bibitem{sammarra2007} M. Sammarra et al. Quay crane scheduling. \textit{Journal of Scheduling}, 2007.
\bibitem{zhang2002} C. Zhang et al. Dynamic crane deployment. \textit{Transportation Research Part B}, 2002.
\bibitem{vaswani2017} A. Vaswani et al. Attention Is All You Need. \textit{NeurIPS}, 2017.
\bibitem{williams1992} R. J. Williams. REINFORCE. \textit{Machine Learning}, 1992.
\bibitem{caserta2012} M. Caserta et al. Corridor method for BRP. \textit{OR Spectrum}, 2012.
\end{thebibliography}
\end{document}
